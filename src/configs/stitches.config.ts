// stitches.config.ts
import { createStitches } from '@stitches/react';

export const {styled} = createStitches({
    theme: {
        colors: {
            defaultLightBlue: "#0ecaff",
            darkGrey: "#303030",
            white: "#fffff"
        },
    
    },
});

